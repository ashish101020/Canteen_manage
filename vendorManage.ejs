<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }

        h1, h3 {
            text-align: center;
            color: #333;
        }

        table {
            width: 100%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #fff;
        }

        th, td {
            padding: 10px;
            border: 1px solid #ccc;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        a {
            text-decoration: none;
            color: #007bff;
        }

        a:hover {
            text-decoration: underline;
        }

        .no-vendors {
            text-align: center;
            color: #777;
        }

        /* Responsive Styles */
        @media screen and (max-width: 768px) {
            table {
                width: 90%;
            }
        }

        @media screen and (max-width: 576px) {
            table {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <h1>Vendors List</h1>
    <table>
        <tr>
            <th>Vendor ID</th>
            <th>Name</th>
            <th>Vendor Type</th>
            <th>Phone</th>
            <th>Action</th>
            <th>Data</th>
        </tr>
        <% if(vendors.length > 0){ %>
            <% for(var i=0; i<vendors.length; i++){ %>
                <tr>
                    <td><%- vendors[i].vendor_id %></td>
                    <td><%- vendors[i].vendor_name %></td>
                    <td><%- vendors[i].vendor_type %></td>
                    <td><%- vendors[i].phone %></td>
                    <td><a href="/addProduct/<%= vendors[i].vendor_id %>">Add Product</a></td>
                    <td><a href="/purchased-Product/<%= vendors[i].vendor_id %>">Purchased Data</a></td>
                </tr>
            <%}%>
        <% } else { %>
            <tr>
                <td colspan="6" class="no-vendors">No vendors found</td>
            </tr>
        <% } %>
    </table>
    <h3>Add Vendor</h3>
    <p style="text-align: center;"><a href="/add-vendor">Add Vendor</a></p>
</body>
</html>
